<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itsq.mapper.PlayerOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.itsq.pojo.entity.PlayerOrder">
        <id column="id" property="id" />
        <result column="number" property="number" />
        <result column="player_id" property="playerId" />
        <result column="arms_id" property="armsId" />
        <result column="is_status" property="isStatus" />
        <result column="buyPrice" property="buyPrice" />
        <result column="cre_date" property="creDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, number, player_id, arms_id, is_status, buyPrice, cre_date
    </sql>

</mapper>
